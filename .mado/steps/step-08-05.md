# Phase 8 — Step 8.5: Docs correction + release-build perf & mobile verification

## Objective

Close out Phase 8: correct the documentation inaccuracies surfaced by the QA review (**DOC-01**), then run the
two deferred verification passes (**FU-01** release-build perf, **FU-02** real mobile-viewport check) and a
regression sweep so the fixes from steps 8.1–8.4 are confirmed on a production-like build. Reference:
[`docs/QA_E2E_FINDINGS.md`](../../docs/QA_E2E_FINDINGS.md) DOC-01, FU-01, FU-02.

## Dependencies

- **Requires:** Steps 8.1–8.4 complete (the fixes to verify). Uses `docs/DEMO.md`, `docs/DESIGN.md`,
  `docs/QA_E2E_FINDINGS.md`.

## Tasks

Complete in order:

1. **DOC-01 — Correct DEMO.md.**
    - §7: the claim that "because requests carry `Accept-Language`, the API copy follows" the language toggle
      was previously untrue for content strings. After step 8.1 it is true — update the wording to reflect the
      now-correct behaviour (and remove the two "honest notes" caveats only if they no longer apply).
    - §9: correct the bank-report step — the report is generated by clicking an explicit **"Generate predictive
      report"** button (not auto-generated on applicant select).
    - Re-check the persona figures quoted in DEMO.md against the current live API and update any that drifted.

2. **FU-01 — Release-build performance pass.**
    - Build the web app in release: `flutter build web` (release), serve the built bundle, and re-run the
      key flows. Confirm the per-screen loading spinners/stalls seen under `flutter run` (debug) are gone and
      capture rough front-end timings. Confirm backend analytics stay <2.5 s (DESIGN §10 NFR).

3. **FU-02 — Real mobile-viewport check.**
    - Run the consumer shell on a phone-sized viewport (device/emulator or `flutter run -d chrome` at a phone
      window) and confirm the mobile-first layout is correct and that step 8.2's width-frame is a no-op at
      phone width (no regression).

4. **Regression sweep of Phase 8 fixes.**
    - Re-verify: Arabic mode shows Arabic content everywhere (8.1); wide-web frame + bounded gauge (8.2);
      pluralization, recovery gauge, velocity colour, locale persistence (8.3); queue decision + portfolio
      bands (8.4). Run the full backend + frontend test suites.

5. **Update the findings log.**
    - In `docs/QA_E2E_FINDINGS.md`, mark each item's Status (Fixed / Won't-fix / Deferred) with the step that
      resolved it, so the report reflects the final state.

## Acceptance Criteria

- [ ] `docs/DEMO.md` §7 and §9 match actual behaviour; quoted persona figures are current.
- [ ] A `flutter build web` release bundle runs the flows without the debug-only spinners; backend analytics
      confirmed <2.5 s.
- [ ] The consumer shell verified at phone width with no layout regression from step 8.2.
- [ ] All Phase 8 fixes re-verified; full backend + frontend test suites pass.
- [ ] `docs/QA_E2E_FINDINGS.md` statuses updated to reflect resolution.

## Files to Create or Modify

| Action | File Path | Notes |
|--------|-----------|-------|
| Modify | `docs/DEMO.md` | Fix §7 (i18n) and §9 (report button); refresh figures |
| Modify | `docs/QA_E2E_FINDINGS.md` | Set final Status per finding |
| (Verify only) | web release build + test suites | No code changes expected unless verification finds a regression |

## Notes

- This is the verification/close-out step — if any acceptance check fails, mark Phase 8 `blocked`/`failed` in
  STATUS.json, note the regressing item in a handoff, and stop (per AGENT.md) rather than patching outside the
  step.
- No new features here; scope is docs + verification of 8.1–8.4 only.

# Phase 3 — Step 3.5: Flutter Simulate screen (loan + chat tabs)

## Objective

Build the consumer **Simulate** screen (DESIGN §7.2 & §8) with two tabs — **Loan Affordability**
(live-computed sliders) and **Ask Baseerah AI** (chat) — driven entirely by the Phase 3 backend
endpoints, RTL-correct and faithful to the prototype visual system.

## Dependencies

- **Requires:** Step 3.3 — `POST /api/v1/clients/{id}/loan-simulate` must be live.
- **Requires:** Step 3.4 — `POST /api/v1/clients/{id}/chat` and `.../chat/invoice` must be live.
- **Requires:** Step 0.3 — the Flutter skeleton (theme tokens, i18n/RTL, routing, dio client) must exist.
- **Assumes:** the bottom nav (Home · Simulate · Rescue · Goals) from Phase 2 routes to this screen.

## Tasks

Complete in order:

1. **Simulate screen shell with two tabs**
    - Create the Simulate screen under `frontend/` with a tab control: **Loan Affordability** and
      **Ask Baseerah AI** (DESIGN §7.2). All colours/fonts/radii from the single theme token file (DESIGN §8);
      RTL-correct for Arabic.

2. **Loan Affordability tab (live sliders)**
    - Three sliders: **principal**, **rate**, **term** (DESIGN §7.2).
    - On change (debounced), call `loan-simulate` via a dio-backed provider/repo and display **live**
      `installment`, `DTI` (with `dtiColor`), `verdict` (with `verdictColor`), and **score impact**
      (`projectedScore`).
    - Format money with the `money()` helper (SAR / ر.س); verdict/DTI use the returned hex colours
      (green `#1D9E63` / orange `#E5A63A` / red `#E0574F`).

3. **Ask Baseerah AI tab (chat)**
    - Chat bubble list: **user right-aligned, AI left-aligned** (mirror correctly under RTL).
    - **Typing indicator:** blinking dots using the `bsr-blink` (1s) animation while awaiting the reply
      (DESIGN §8).
    - **Suggestion chips** that, when tapped, send that text as a message.
    - Text input + **send** button calling `POST .../chat`.
    - **Invoice upload** control (image picker) calling `POST .../chat/invoice` (multipart), then showing
      the returned parsed-action stub as an AI bubble.

4. **State + wiring**
    - Use the project's chosen state management (Riverpod per DESIGN §2) for slider state, chat history,
      and loading/typing state. Keep API calls in a repository, not in widgets.

## Acceptance Criteria

All the following must be true before marking this step complete:

- [ ] Moving any of the three sliders updates `installment`, `DTI`, `verdict`, and score impact **live**
      from the `loan-simulate` API, with colours matching the returned `dtiColor`/`verdictColor`.
- [ ] The chat tab shows user bubbles (right) and AI bubbles (left) and displays the blinking `bsr-blink`
      typing indicator while awaiting a reply.
- [ ] Tapping a suggestion chip sends that text and appends the AI reply.
- [ ] Invoice upload sends the image (multipart) and renders the returned parsed-action stub.
- [ ] The entire screen is RTL-correct in Arabic (bubble alignment, sliders, tab order) and uses only
      theme tokens for colours/typography.

## Files to Create or Modify

| Action | File Path | Notes |
|--------|-----------|-------|
| Create | `frontend/` Simulate screen widget | Two-tab shell (DESIGN §7.2) |
| Create | `frontend/` Loan Affordability tab + sliders widget | Live `loan-simulate` results |
| Create | `frontend/` Ask Baseerah AI chat tab widget | Bubbles, `bsr-blink` dots, chips, invoice upload |
| Create | `frontend/` simulate repository + providers | dio calls to loan/chat endpoints |
| Modify | `frontend/` router / bottom-nav | Route Simulate tab to this screen |

## Notes

- Reproduce the prototype look (DESIGN §8): card radii, teal/gold palette, `bsr-up` entrance where
  appropriate. Do not modify `design/` prototype files — mirror them in Flutter.
- Debounce slider-driven API calls to keep interactions smooth and stay within the < 2.5 s analytics NFR
  under rapid dragging.
- The mock chat is instant (Step 3.4), so the typing indicator will be brief; keep it visible long enough
  to read as intentional feedback.

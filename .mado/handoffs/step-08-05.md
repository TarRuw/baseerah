# Session Handoff — Step 8.5: Docs correction + release-build perf & mobile verification

**Date:** 2026-07-08
**Status:** completed — **Phase 8 complete, project 35/35 (100%).** This is the final step. Closes QA finding
**DOC-01** and the two follow-ups **FU-01** (release-build perf) and **FU-02** (mobile viewport); re-verifies
the 8.1–8.4 fixes on a production-like build. Full suites green: **backend 108/108** (0 failures/errors,
`--rerun-tasks`), **frontend 28/28** (`flutter test`, all passed).

## What was done

Verification/close-out only — **no application code changed**; edits are docs (`DEMO.md`,
`QA_E2E_FINDINGS.md`) plus the STATUS.json bookkeeping. All verification ran against the **live backend**
(`./gradlew bootRun`, Postgres container) with the frontend served as a **`flutter build web --release`**
bundle driven in Chrome.

### DOC-01 — `docs/DEMO.md` corrected (verified against the live API)
- **§7 (language toggle):** reworded to state that server-provided **content** now follows `Accept-Language`
  (true after step 8.1), naming the fields that switch (loan verdict, Rescue labels/details + confirm line,
  challenge copy, Ask-Baseerah reply) and noting proper-noun seed data (persona names) stays fixed. Added that
  locale now survives a full reload (8.3). Left the two "honest notes" in place — **both still hold live**
  (tech_bro computes 92/OPTIMAL, not warning; freelancer `alertRaised:false`, deficit ~2508 days out).
- **§9 (bank report):** corrected — selecting an applicant does **not** auto-generate; the pane shows a
  **"Generate predictive report"** button (`bankGenerateReport`) that must be clicked to trigger the
  "Analyzing 24-month telemetry…" spinner → report. Verified in the running UI.
- **Persona figures refreshed** where drifted vs the live API: freelancer shortfall **~SAR 2,779 → ~SAR 2,943**
  (two occurrences), Murabaha bridge **~SAR 2,800 → ~SAR 3,000**. Re-confirmed still-accurate: family 95/OPTIMAL,
  tech_bro 92, loan installment ~1,510 / DTI ~9%, recovery 83 → 91.

### FU-01 — Release-build perf pass — **Done**
Served the release bundle, drove Home → Simulate (Loan + Ask-Baseerah) → Bank (Applications, report,
Approve). The debug-only per-screen spinners/stalls were **gone** — every screen/transition rendered without a
debug stall. Backend analytics far under the 2.5 s NFR: stress-score ≈ 4–5 ms, bank report ≈ 8 ms, 30-day
forecast ≈ 3 ms (all 200). Confirms the debug-web JIT was the only source of perceived slowness (as the QA
report predicted).

### FU-02 — Mobile-viewport check — **Done**
The Chrome window would not shrink below the desktop minimum (`window.innerWidth` stayed 1542 — the same
harness limit the QA report flagged), so the app was rendered inside a **390×844 iframe** to give Flutter a
true phone `MediaQuery`. At phone width the mobile-first layout is correct and **full-bleed** — step 8.2's
max-width phone-frame is a **no-op** (no letterbox, no regression). Gauge settles at 95, all sections lay out
correctly. Corroborated by `responsive_frame_test`.

### Regression sweep — all Phase 8 fixes re-verified
- **8.1 (I18N-01, COPY-01):** live API + UI confirm ar/en switching of loan verdict (`ميسور التكلفة`), Rescue
  options + confirm line, chat reply, and bank verdict (`يتحمّل الدين`). `category.RESTAURANTS_DINING` grammar
  fixed to "restaurant dining".
- **8.2 (UI-01):** phone-frame on wide web + no-op at phone width — visual + `responsive_frame_test`.
- **8.3 (UI-02/03/04/07):** pluralization confirmed live (`مباشر · بنك واحد`); recovery gauge rests on 91
  (`recovery_gauge_test`); velocity colour + locale persistence covered by suite/fixes.
- **8.4 (UI-05/06):** approving Khalid Al-Otaibi flipped his queue chip to `موافق عليه` in-session (a
  `GET /bank/applicants` fires right after the decision POST); portfolio bands covered by widget test.
- **Findings log:** every item's Status set to **Fixed** with the resolving step; FU-01/FU-02 marked **Done**
  with results; a resolution banner added at the top.

## Key decisions / notes
- **Kept the two DEMO §7 "honest notes"** rather than deleting them — the step said remove *only if they no
  longer apply*, and both (tech_bro OPTIMAL≈92; freelancer long-horizon `alertRaised:false`) remain live-true.
- **iframe over window-resize for FU-02:** the harness cannot resize below desktop min; embedding in a
  390 px iframe gives Flutter a genuine phone `MediaQuery`, which is the property under test (not the OS window).
- **Persona figures drift:** the shortfall/Murabaha numbers moved since the QA report was written (seeded-data
  derived, non-hardcoded per the Global Rule); DEMO now matches the current live values.

## Project status
**Baseerah MADO plan complete — 35/35 steps, all 8 phases done.** No pending steps remain in STATUS.json. All
QA findings (UI-01, I18N-01, UI-02..07, COPY-01, DOC-01) closed and both follow-ups done.
